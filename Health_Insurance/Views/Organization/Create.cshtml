@model Health_Insurance.Models.Organization
@* Ensure the model namespace is correct based on your project name *@

@{
    ViewData["Title"] = "Create Organization";
}

<h1 class="mb-4">Create Organization</h1>

<h4>Organization</h4>
<hr />
<div class="row">
    <div class="col-md-6">
        <form asp-action="Create">
            @Html.AntiForgeryToken()
            <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

            <div class="form-group mb-3">
                <label asp-for="OrganizationName" class="form-label"></label>
                <input asp-for="OrganizationName" class="form-control" />
                <span asp-validation-for="OrganizationName" class="text-danger"></span>
            </div>
            <div class="form-group mb-3">
                <label asp-for="ContactPerson" class="form-label"></label>
                <input asp-for="ContactPerson" class="form-control" />
                <span asp-validation-for="ContactPerson" class="text-danger"></span>
            </div>
            @* --- REMOVED Phone Number input --- *@
            @*
            <div class="form-group mb-3">
                <label asp-for="PhoneNumber" class="form-label"></label>
                <input asp-for="PhoneNumber" class="form-control" />
                <span asp-validation-for="PhoneNumber" class="text-danger"></span>
            </div>
            *@
            <div class="form-group mb-3">
                <label asp-for="ContactEmail" class="form-label"></label> @* --- RENAMED asp-for --- *@
                <input asp-for="ContactEmail" class="form-control" /> @* --- RENAMED asp-for --- *@
                <span asp-validation-for="ContactEmail" class="text-danger"></span> @* --- RENAMED asp-validation-for --- *@
            </div>
            <div class="form-group mt-3">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div class="mt-3">
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
