@model Health_Insurance.Models.HR
@* Ensure the model namespace is correct based on your project name *@

@{
    ViewData["Title"] = "HR Details";
}

<h1 class="mb-4">HR Details</h1>

<div>
    <h4>HR Personnel</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Name)</dt>
        <dd class="col-sm-9">@Html.DisplayFor(model => model.Name)</dd>

        <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Email)</dt>
        <dd class="col-sm-9">@Html.DisplayFor(model => model.Email)</dd>

        <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Phone)</dt>
        <dd class="col-sm-9">@Html.DisplayFor(model => model.Phone)</dd>

        <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Username)</dt>
        <dd class="col-sm-9">@Html.DisplayFor(model => model.Username)</dd>
    </dl>
</div>
<div class="mt-3">
    <a asp-action="Edit" asp-route-id="@Model?.HRId" class="btn btn-info me-2">Edit</a>
    <a asp-action="Index" class="btn btn-secondary">Back to List</a>
</div>

